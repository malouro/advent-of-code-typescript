/** @todo - Implement? Is this needed even? */
export function runSolver(day: number) {}

if (require.main === module) {
  // collect args and run the function above to execute the solution for any day.
}
